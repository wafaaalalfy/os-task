on:
  push:
    branche:
      - main 








jobs:
  build:
    runs-on: ubuntu-latest
     

    steps:
      - uses: actions/checkout@v3
      
      - name: "login to Docker hub "
        run: docker login --username wafaaalalfy --password dckr_pat_BWUIpymek098XAclvnq34Ha41S8
      
     